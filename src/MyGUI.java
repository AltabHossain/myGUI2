/*
*
*/

import javax.swing.*;
import java.awt.event.*;
import java.awt.*;
public class MyGUI {
    public static void main(String[] args) {
        ImageIcon backgroundImg;
        ImageIcon backgroundImg2 = new ImageIcon("levelBg.png");;
        MyFrame frame = new MyFrame("myGUI");

       /* JPanel background = new JPanel();

        background.setBounds(0, 0, 700, 500);
        background.setOpaque(false);

        backgroundImg = new ImageIcon("levelBg.png");
        JLabel bgLabel = new JLabel(backgroundImg);
        bgLabel.setSize(700, 500);
        bgLabel.add(background);

        JPanel buttons = new JPanel();
        buttons.setBounds(10, 170, 300, 146);
        frame.add(buttons);*/
    }
}